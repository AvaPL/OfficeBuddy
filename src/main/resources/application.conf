postgres {
  host = "0.0.0.0"
  port = 2345
  user = "postgres"
  password = "postgres"
  database = "office_buddy"
  max-concurrent-sessions = 10
}

http {
  host = "0.0.0.0"
  port = 8080
  internal-api-base-path = ["api", "internal"]
}